The .deb in this folder is included in this handoff so it is easy to publish.
Do NOT commit the releases/ directory to Git history.
The publish script uploads the .deb as a GitHub Release asset instead.
